services:
  api:
    build:
      context: ./back-end
      dockerfile: Dockerfile
    container_name: scratch-card-api
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
